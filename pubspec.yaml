name: areyousure
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.7.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^7.0.0
  screenshot: ^1.2.3
  cupertino_icons: ^1.0.8
  animations: ^2.0.8
  flare_flutter: ^3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/logo2.png"  # Path to your app icon


flutter:
  uses-material-design: true
  
  assets:
    - assets/images/gg.jpg
    - assets/images/