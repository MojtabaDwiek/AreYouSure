name: areyousure
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  share_plus: ^7.0.0
  screenshot: ^1.2.3
  cupertino_icons: ^1.0.8
  # Add these new dependencies for enhanced animations
  animations: ^2.0.8
  flare_flutter: 3.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/gg.jpg
    - assets/images/  # Include whole directory if adding more images later

  # Uncomment and customize if you want to add custom fonts
  # fonts:
  #   - family: YourCustomFont
  #     fonts:
  #       - asset: assets/fonts/YourFont-Regular.ttf
  #       - asset: assets/fonts/YourFont-Bold.ttf
  #         weight: 700